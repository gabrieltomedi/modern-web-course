const printResult = function (grade) {
    if (grade >= 7) {
        console.log('Approved with grade ' + grade)
    } else {
        console.log('Approved with grade ' + grade)
    }
}

printResult(10)
printResult(4)
printResult('test!')