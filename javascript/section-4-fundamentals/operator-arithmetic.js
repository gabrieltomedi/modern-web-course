const [a, b, c, d] = [3, 5, 1, 15] //Creating multiple variables at once with destructuring

const sum = a + b + c + d
const subtraction = d - b
const multiplication = a * b
const division = d / a
const module = a % 2

console.log(sum, subtraction, multiplication, division, module)

